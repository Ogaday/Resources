# Collect list of samples already run on the computer
# ie. cases = [entry.name for entry in os.scandir() if finished(entry.name)] etc.
# Then for each case, extract the already calculated power in the central spreadsheet, compare to within enough decimal places accuracy. There is some discrepency at 10e-14.
# Done! 

# Also test all of the utility methods as well.

pass
